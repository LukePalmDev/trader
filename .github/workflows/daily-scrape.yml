name: Daily Scrape

on:
  schedule:
    # 05:00 UTC = 06:00 CET (inverno) / 07:00 CEST (estate)
    - cron: '0 5 * * *'
  workflow_dispatch:  # trigger manuale dalla UI GitHub

jobs:
  scrape:
    runs-on: ubuntu-latest
    env:
      TRADER_PLAYWRIGHT_CHANNEL: chromium
      TRADER_VIEWER_OPEN_BROWSER: "false"
    permissions:
      contents: write   # serve per fare git push

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install Python dependencies (locked)
        run: pip install -r requirements.lock

      - name: Install Playwright browsers
        run: |
          python -m playwright install chromium
          python -m playwright install-deps chromium

      - name: Run scrapers
        run: python run.py

      - name: Cleanup and archive old data
        run: python run.py --cleanup

      - name: Commit and push DB/artifacts
        run: |
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add trader.db subito.db ebay.db
          git add logs/run_report_latest.json || true
          git diff --staged --quiet \
            || git commit -m "chore: scrape $(date -u +%Y-%m-%d)"
          git push
